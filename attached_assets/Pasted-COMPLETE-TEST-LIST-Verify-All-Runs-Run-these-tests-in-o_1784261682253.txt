COMPLETE TEST LIST — Verify All Runs

Run these tests in order. Each test confirms a specific run worked correctly.

---

SECTION 1: Template Registry & Rendering (RUN 2)

Test 1.1 — Template Registry Exists

Action: Check that app/services/notification_templates.py exists
Expected: File exists with NOTIFICATION_TEMPLATES dict containing all notification types

Test 1.2 — All 35 Email Types Have Templates

Action: Verify every type in EMAIL_TYPES has a corresponding entry in NOTIFICATION_TEMPLATES
Expected: No missing templates. Each type has: channels, email, personalized, critical_fields, fallbacks

Test 1.3 — render_notification_template() Works

Action: Call render_notification_template("order_confirmed", {"order_id": "123", "order_ref": "ORD-001", "name": "John"})
Expected: Returns (title, body, channels, email) with {order_id} and {order_ref} replaced with values

Test 1.4 — render_notification_template() Fails Gracefully

Action: Call render_notification_template("order_confirmed", {}) (missing critical fields)
Expected: Returns None or raises clear error about missing critical fields

---

SECTION 2: Personalization — {name} & include_name (RUN 3)

Test 2.1 — Personalized Notification Gets Name

Action: Call send_notification(user_id, "order_confirmed", template_data={"order_id": "123", "order_ref": "ORD-001"})
Expected: The rendered title/body contains the user's name (e.g., "Hey John, your order #123 is confirmed!")

Test 2.2 — Non-Personalized Notification Does NOT Get Name

Action: Call send_notification(user_id, "password_reset", template_data={"reset_link": "https://..."})
Expected: Rendered title/body does NOT contain the user's name

Test 2.3 — Admin Blast {name} Replacement

Action: Send admin blast with message: "Hey {name}, check out our new menu!"
Expected: User John sees: "Hey John, check out our new menu!"; User without name sees: "Hey there, check out our new menu!"

Test 2.4 — include_name Parameter Override

Action: Call send_notification(user_id, "order_confirmed", template_data={...}, include_name=False)
Expected: No name is injected, even though template is marked as personalized

---

SECTION 3: Critical Fields Validation & Fallbacks (RUN 4)

Test 3.1 — Send Succeeds When All Critical Fields Present

Action: Call send_notification(user_id, "order_confirmed", template_data={"order_id": "123", "order_ref": "ORD-001"})
Expected: Notification sends successfully

Test 3.2 — Send SKIPPED When Critical Field Missing

Action: Call send_notification(user_id, "order_confirmed", template_data={"order_ref": "ORD-001"}) (missing order_id)
Expected: Notification does NOT send. Returns None. Logs warning: "Skipping order_confirmed for user X: missing critical fields: order_id"

Test 3.3 — Send SKIPPED When HP Amount Missing

Action: Call send_notification(user_id, "hp_gift_received", template_data={"gift_sender": "Jane"}) (missing hp_amount)
Expected: Notification does NOT send. Returns None. Logs warning.

Test 3.4 — Fallback Applied for Non-Critical {name}

Action: Call send_notification(user_id, "login_streak_checkin", template_data={"streak_count": 5}) (missing name)
Expected: Notification sends with {name} replaced with "there" (e.g., "You checked in 5 days in a row, there. Keep it up!")

Test 3.5 — Fallback Applied for Non-Critical {tier_name}

Action: Call send_notification(user_id, "tier_upgrade", template_data={}) (missing tier_name)
Expected: Notification sends with {tier_name} replaced with "your tier"

Test 3.6 — Fallback Applied for Non-Critical {streak_count}

Action: Call send_notification(user_id, "login_streak_checkin", template_data={"name": "John"}) (missing streak_count)
Expected: Notification sends with {streak_count} replaced with "your streak"

Test 3.7 — Fallback Applied for Non-Critical {gift_sender}

Action: Call send_notification(user_id, "hp_gift_received", template_data={"hp_amount": 100}) (missing gift_sender)
Expected: Notification sends with {gift_sender} replaced with "someone"

---

SECTION 4: Email Priority — EMAIL_TYPES (RUN 5)

Test 4.1 — EMAIL_TYPES Has 35 Types

Action: Count the number of types in EMAIL_TYPES in notification_service.py
Expected: Exactly 35 types

Test 4.2 — New Email Types Are Present

Action: Check that these types exist in EMAIL_TYPES:

· order_cancelled_user
· order_cancelled_admin
· scheduled_order_cancelled
· guest_order_claimed
· wallet_funded_card
· wallet_funded_bank
· hp_gift_received
· hp_transfer_recipient
· hp_decay_applied
· hp_decay_warning
· winback_118
· tier_downgrade
· graduation_declared
· event_registered
· marketplace_purchase
· reward_fulfilled
· password_changed
· password_reset
· email_verification
· account_deleted
· account_deactivated
· account_reactivated
· squad_member_invite

Expected: All 23 new types are present

Test 4.3 — Removed Email Types Are GONE

Action: Check that these types are NOT in EMAIL_TYPES:

· order_delivery_attempted
· hp_earned
· referral_hp_earned
· tier_dropped
· winback_70

Expected: All 5 removed types are absent

Test 4.4 — Wallet Withdrawal Types Are GONE

Action: Check that these types are NOT in EMAIL_TYPES:

· wallet_withdrawal_submitted
· wallet_withdrawal_approved
· wallet_withdrawal_rejected

Expected: All 3 types are absent

Test 4.5 — Email Actually Sends for Email Types

Action: Trigger a notification with type order_confirmed (which is in EMAIL_TYPES)
Expected: Email is sent (in addition to In-App + Push)

Test 4.6 — Email NOT Sent for Non-Email Types

Action: Trigger a notification with type that is NOT in EMAIL_TYPES (e.g., order_status_preparing if not in EMAIL_TYPES)
Expected: Only In-App + Push are sent. No email.

---

SECTION 5: All Callers Migrated (RUN 6)

Test 5.1 — No MSG.format() Calls Remain

Action: Run grep -r "MSG\.[A-Z_]*\.format" app/ --include="*.py"
Expected: 0 results — no call sites still using old MSG.format() pattern

Test 5.2 — All Call Sites Use template_data

Action: Check a sample of files for template_data={
Expected: Each file has at least one template_data={...} call

Test 5.3 — Order Confirmed Call Site

Action: Place an order that triggers order_confirmed notification
Expected: Notification sends with template_data containing order_id and order_ref

Test 5.4 — Birthday Bonus Call Site

Action: Trigger birthday bonus for a user (or run scheduled task manually)
Expected: Notification sends with template_data containing name and hp

Test 5.5 — Referral HP Earned Call Site

Action: Refer a user who completes a qualifying action
Expected: Notification sends with template_data containing hp and referral_count

---

SECTION 6: Admin Blasts Enhancements (RUN 7)

Test 6.1 — All 17 Filters Are Present

Action: Check admin blast creation UI or API for filter options
Expected: All 17 filters exist (Tier, Role, Department, Faculty, Has Pending HP, HP Balance, Last Login, Last Order, Total Orders, Has Referral, Has Squad Order, Has Reviewed, Has Shared, Event Attendance, Has Graduated, Level in Department, Level in General)

Test 6.2 — {name} Replacement in Admin Blasts

Action: Send admin blast with message "Hey {name}, special offer for you!"
Expected: Recipient sees their name (e.g., "Hey John, special offer for you!")

Test 6.3 — {name} Fallback in Admin Blasts

Action: Send admin blast to a user without a full_name
Expected: They see "Hey there, special offer for you!"

Test 6.4 — Filter by Department Works

Action: Create admin blast targeting a specific department
Expected: Only users in that department receive the blast

Test 6.5 — Filter by Faculty Works

Action: Create admin blast targeting a specific faculty
Expected: Only users in departments mapped to that faculty receive the blast

Test 6.6 — Filter by Level in Department Works

Action: Create admin blast targeting "200lv Computer Science"
Expected: Only users with department Computer Science AND level 200 receive the blast

Test 6.7 — Filter by Level in General Works

Action: Create admin blast targeting "200lv" (all departments)
Expected: Only users with level 200 receive the blast (regardless of department)

Test 6.8 — Filter by HP Balance Works

Action: Create admin blast targeting "HP Balance: Low"
Expected: Only users with low HP balance receive the blast

---

SECTION 7: New Post-Delivery Notifications (RUN 8)

Test 7.1 — Thank You Message on Order Delivery

Action: Mark an order as delivered
Expected: User receives In-App + Push notification: "Thank You Message" with {name} personalized

Test 7.2 — Satisfaction Check 2 Hours After Delivery

Action: Wait 2 hours after order delivery (or manually trigger the scheduled task)
Expected: User receives In-App + Push notification: "Satisfaction Check" with {name} personalized

Test 7.3 — Re-engagement Nudge 24 Hours After Delivery

Action: Wait 24 hours after order delivery (or manually trigger the scheduled task)
Expected: User receives In-App ONLY notification: "Re-engagement Nudge" with {name} personalized

Test 7.4 — All 3 Types Are in Template Registry

Action: Check NOTIFICATION_TEMPLATES for types: order_thank_you, satisfaction_check, reengagement_nudge
Expected: All 3 are present with correct metadata

---

SECTION 8: Department/Faculty Feature (RUN 9)

Test 8.1 — Departments Table Exists

Action: Query SELECT * FROM departments LIMIT 1 in Supabase
Expected: Table exists (may be empty)

Test 8.2 — department_id Column Exists in profiles

Action: Query SELECT column_name FROM information_schema.columns WHERE table_name='profiles' AND column_name='department_id'
Expected: Column exists

Test 8.3 — Admin Can Create Department

Action: POST to /api/admin/departments with {"name": "Computer Science", "slug": "computer-science", "faculty": "Technology"}
Expected: Returns 201, department created

Test 8.4 — Admin Can Map Department to Faculty

Action: PATCH to /api/admin/departments/{id} with {"faculty": "Engineering"}
Expected: Department faculty updated

Test 8.5 — User Can Select Department at Registration

Action: Register new user with department_id set
Expected: User profile has department_id set correctly

Test 8.6 — User Can Select Department in Profile Update

Action: PATCH to /api/profile with department_id
Expected: Profile updated correctly

Test 8.7 — Faculty Is Derived, Not User-Selected

Action: User selects "Computer Science" department (faculty = "Technology")
Expected: User profile shows faculty = "Technology" (derived). User cannot set faculty directly.

Test 8.8 — Admin Blast Filters by Department

Action: Create admin blast with filter: Department = "Computer Science"
Expected: Only users in Computer Science department receive the blast

Test 8.9 — Admin Blast Filters by Faculty

Action: Create admin blast with filter: Faculty = "Technology"
Expected: Only users in departments mapped to "Technology" receive the blast

---

SECTION 9: Follow-Up Tasks (RUN 10)

Test 9.1 — Event Registration Idempotent

Action:

1. Register user for event → should succeed (200)
2. Register SAME user for SAME event again → should return 200 with existing ticket (NOT 400)

Expected: Second registration returns 200 with existing ticket. Tests 16.3 and 16.4 pass on every re-run.

Test 9.2 — HP Transfer Completed-Order Count

Action:

1. Place and deliver an order
2. Immediately check completed_count via HP transfer endpoint

Expected: completed_count shows the delivered order (NOT 0). The minimum order threshold (default 3) is clearly documented in the API error.

Test 9.3 — Paystack Virtual Account Fallback

Action: Call POST /api/wallet/fund/bank in sandbox mode with PAYSTACK_SANDBOX_MOCK_NUBAN=true
Expected: Returns mock virtual account response (NOT 502). Clear error message explaining sandbox limitations.

---

SECTION 10: Wallet Withdrawals Removal (RUN 11)

Test 10.1 — POST /wallet/withdraw Route Is Gone

Action: POST to /wallet/withdraw
Expected: Returns 404 (route not found)

Test 10.2 — request_withdrawal() Function Is Gone

Action: Search codebase for def request_withdrawal
Expected: No results

Test 10.3 — wallet_withdrawal Service Function Is Gone

Action: Search codebase for def wallet_withdrawal
Expected: No results

Test 10.4 — Withdrawal Notification Types Are Gone

Action: Search codebase for wallet_withdrawal_submitted, wallet_withdrawal_approved, wallet_withdrawal_rejected
Expected: No results in notification types

Test 10.5 — Withdrawal Email Template Is Gone

Action: Search email.py for withdrawal-related templates
Expected: No withdrawal email templates exist

Test 10.6 — Swagger/OpenAPI Docs Are Clean

Action: Check API docs for /wallet/withdraw
Expected: No withdrawal endpoint documented

---

SECTION 11: Hardcoded Values Verification (RUN 1 — DO LAST)

Test 11.1 — HP Bundle Name Is Not Hardcoded

Action: Search codebase for hardcoded HP bundle name (e.g., "Holy Grills" or specific bundle names)
Expected: 0 results. Bundle name comes from config/database.

Test 11.2 — HP Bundle Price Is Not Hardcoded

Action: Search codebase for hardcoded bundle prices
Expected: 0 results. Prices come from config/database.

Test 11.3 — HP Bundle Amount Is Not Hardcoded

Action: Search codebase for hardcoded bundle amounts
Expected: 0 results. Amounts come from config/database.

Test 11.4 — {platform} Used Instead of Hardcoded App Name

Action: Search codebase for "Holy Grills" in any MSG string
Expected: 0 results. All use {platform} placeholder which resolves to APP_NAME config.

Test 11.5 — All Hardcoded Values Are Moved to Config/MSG

Action: Review all MSG constants and config values
Expected: No bare literals in user-facing strings. All strings are MSG constants or config values.

---

EXECUTION ORDER

Order Section What It Verifies
1 SECTION 1 RUN 2 — Template Registry
2 SECTION 2 RUN 3 — Personalization
3 SECTION 3 RUN 4 — Critical Fields & Fallbacks
4 SECTION 4 RUN 5 — EMAIL_TYPES
5 SECTION 5 RUN 6 — All Callers Migrated
6 SECTION 6 RUN 7 — Admin Blasts
7 SECTION 7 RUN 8 — New Notifications
8 SECTION 8 RUN 9 — Departments
9 SECTION 9 RUN 10 — Follow-Up Tasks
10 SECTION 10 RUN 11 — Wallet Withdrawals Removal
11 SECTION 11 RUN 1 — Hardcoded Values (DO LAST)

---

QUICK VERIFICATION COMMANDS

Run these in terminal to quickly check the most critical items:

```bash
# Check EMAIL_TYPES count
grep -c '"' app/services/notification_service.py | grep EMAIL_TYPES -A 50

# Check no MSG.format calls remain
grep -r "MSG\.[A-Z_]*\.format" app/ --include="*.py" | wc -l

# Check no wallet withdrawal endpoints remain
grep -r "/wallet/withdraw" app/ --include="*.py"

# Check no hardcoded "Holy Grills" in MSG strings
grep -r '"Holy Grills"' app/ --include="*.py"

# Check departments table exists (run in Supabase SQL)
SELECT EXISTS (SELECT FROM information_schema.tables WHERE table_name='departments');

# Check department_id column exists (run in Supabase SQL)
SELECT EXISTS (SELECT FROM information_schema.columns WHERE table_name='profiles' AND column_name='department_id');

# Check template registry has 82+ templates
grep -c '"type":' app/services/notification_templates.py
```

---

This is the complete test list. Run each section in order. If any test fails, that run needs attention. 🚀